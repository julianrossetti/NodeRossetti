<div>
    <h1>Estos son todos los productos:</h1>
    {{#each productos}}
        <div>
            <h2>Producto: {{name}}</h2>
            <p>Precio: {{price}}</p>
            <p>Stock: {{stock}}</p>
        </div>
    {{/each}}
</div>